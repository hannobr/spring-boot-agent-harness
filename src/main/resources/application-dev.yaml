spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/template
    username: template
    password: template

jwt:
  secret-key: demo-signing-key-must-be-at-least-256-bits-for-hmac-sha256

cors:
  allowed-origins: http://localhost:5173,http://localhost:3000
